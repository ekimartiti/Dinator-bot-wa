import { DataChunk, InworldPacket as ProtoPacket } from '@proto/packets_pb';
import { Character } from '../entities/character.entity';
import { InworldPacket } from '../entities/inworld_packet.entity';
export declare class EventFactory {
    private character;
    getCurrentCharacter(): Character;
    setCurrentCharacter(character: Character): void;
    dataChunk(chunk: string, type: DataChunk.DataType): ProtoPacket;
    audioSessionStart(): ProtoPacket;
    audioSessionEnd(): ProtoPacket;
    text(text: string): ProtoPacket;
    custom(name: string): ProtoPacket;
    cancelResponse(): ProtoPacket;
    convertToInworldPacket(packet: ProtoPacket): InworldPacket;
    private protoPacket;
    private routing;
    private packet;
    private getType;
    private getControlType;
}
